version: 0.2
phases:
  build:
    commands:
      - echo Entered the build phase...
      - echo Build started on `date`
      - docker build -t hello-world:latest  -f codebuild/Dockerfile codebuild
  post_build:
    commands:
      - echo Build completed on `date`
      - echo Pushing the Docker image...